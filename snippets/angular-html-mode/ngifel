# -*- mode: snippet -*-
# name: NgIf with Then and Else
# key: ngifel
# --
<div *ngIf="${1:cond}; then ${2:thenBlock} else ${3:elseBlock}"></div>
<ng-template #$2>$0</ng-template>
<!-- [$1] else -->
<ng-template #$3></ng-template>