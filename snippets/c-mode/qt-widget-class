# -*- mode: snippet -*-
# name: Define Qt Widget Class
# key: >qw
# --
#ifndef ${1:_`(upcase (file-name-nondirectory (file-name-sans-extension (buffer-file-name))))`_H_}
#define $1

#include <QVBoxLayout>
#include <QWidget>

class ${2:`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`} : public QWidget {
$>Q_OBJECT

public:
$>explicit $2(QWidget* parent = nullptr);$0

private:
$>QVBoxLayout _layout;

private:
$>void _initUI();
};

#endif /* End of $1 */