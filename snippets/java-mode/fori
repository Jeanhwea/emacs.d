# -*- mode: snippet -*-
# name: for (Iterator ...) { ... }
# key: fori
# --
for (Iterator<${1:ClassName}> ${2:it} = $3.iterator(); $2.hasNext(); ) {
$>$1 ${4:${1:$(jh/camelcase yas-text)}} = $2.next();
$>$0
}